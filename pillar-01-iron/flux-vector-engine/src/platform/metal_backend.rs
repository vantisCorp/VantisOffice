//! Metal rendering backend for macOS

/// Metal backend
pub struct MetalBackend {
    // Placeholder for Metal backend implementation
}

impl MetalBackend {
    /// Create a new Metal backend
    pub fn new() -> Result<Self, String> {
        Ok(Self {})
    }
}
